# 2026-06-21

## Django Admin 项目管理功能 & API 增强

### Django Admin (`apps/projects/admin.py`)
- **ProjectAdmin**: 注册 Project 模型，支持内联管理成员(ProjectMemberInline)和环境(ProjectEnvironmentInline)
  - 列表显示: 名称、负责人、状态、成员数、创建时间
  - 搜索: 名称、描述、负责人用户名/邮箱
  - 过滤: 状态、创建时间
  - 可直接在项目编辑页添加/删除成员、更改角色
  - 可直接更改负责人
- **ProjectMemberAdmin**: 注册为独立管理页面，支持按项目/角色过滤
- **ProjectEnvironmentAdmin**: 注册为独立管理页面

### API 新增端点
- **PATCH `/api/projects/<id>/members/<member_id>/`** : 更新成员角色（原 `remove_project_member` 改为 `project_member_detail`，同时支持 DELETE 和 PATCH）
  - 仅项目负责人可操作
  - role 可选: admin/developer/tester/viewer（不能设为 owner）
- **POST `/api/projects/<id>/transfer-owner/`** : 移交项目负责人
  - 仅当前负责人可操作
  - 自动将旧负责人添加为成员（admin 角色）
  - 新负责人如果不是成员，自动添加为成员

### 已有的 API 功能确认
- GET `/api/projects/` : 项目列表（已含权限过滤）
- POST `/api/projects/` : 创建项目
- PUT/PATCH `/api/projects/<id>/` : 更新项目（name/description/status）
- GET `/api/projects/<id>/members/` : 获取成员列表
- POST `/api/projects/<id>/members/add/` : 添加成员

后端已重启

## 修复用例导入功能（3个问题层层排查）

### 问题1: Celery Worker 从未真正启动
- **根因**: `start_celery.bat` 使用了 `--detach` 参数，Windows 不支持（`RuntimeError: This platform does not support detach.`）
- **修复**: 改用 `start /MIN "" venv\Scripts\celery -A backend worker -l info --pool=threads --concurrency=4 --logfile=celery.log`
- 同时改用 `--pool=threads` 避免 Windows 下 prefork 的 PermissionError

### 问题2: 优先级映射不支持 P0/P1/P2/P3
- **现象**: 用户 Excel 中使用 `P0` 等优先级级别，导入服务不支持，172条全部失败
- **修复**: `services.py` PRIORITY_MAP 增加 `p0→critical, p1→high, p2→medium, p3→low`

### 问题3: 不存在的版本引用导致整个行失败
- **现象**: Excel 中引用的版本名如"v2.13迭代"在项目中不存在，导入时 KeyError 崩溃
- **修复**: 版本校验从 `_validate_row` 中移除（不作为硬性错误），导入时只关联存在的版本 (valid_versions 过滤)
- 同时修正 priority/type 校验逻辑：使用 `priority.lower()` 统一比较，移除重复校验

### 最终结果
- 重新派发导入任务：172条全部成功，0失败
- 导入功能现已正常工作

## 批量删除 & 批量修改功能

### 后端新增 API
- **POST `/api/testcases/batch-delete/`** : 批量删除用例，接收 `{"ids": [1,2,3]}`，一次请求完成
  - 只删除用户有权限的项目下的用例
  - 返回 `{deleted_count, not_found_count, message}`
- **POST `/api/testcases/batch-update/`** : 批量修改关联项目和版本，接收 `{"ids": [1,2,3], "project_id": 5, "version_ids": [10,11]}`
  - `project_id` 和 `version_ids` 均为可选，不传则保持原值
  - 自动校验目标和版本是否存在
  - 返回 `{updated_count, not_found_count, message}`

### 新增序列化器 (`serializers.py`)
- `BatchDeleteSerializer`: ids (ListField)
- `BatchUpdateSerializer`: ids + project_id (可选) + version_ids (可选)

### 前端修改 (`TestCaseList.vue`)
- 批量删除：从串行 for 循环改为单次 `POST /testcases/batch-delete/` 请求
- 批量修改：新增"批量修改"按钮和对话框
  - 可选择目标项目和版本
  - 默认值为第一个选中用例的当前参数
  - 切换项目时自动加载对应版本列表（调用 `/versions/projects/{id}/versions/`）
  - 只修改用户明确选择的项，未选择项保持原值

### i18n 新增翻译键
- `batchEdit`, `batchEditTitle`, `batchEditTip`, `batchUpdateSuccess`, `batchUpdateFailed`, `batchDeleteTip`

## 用例导入流程改进 — 版本选择移到页面

### 改动要点
- 模板去除"关联版本"列（从 7 列变 6 列），版本改为页面选择
- 导入时不在 Excel 中校验版本，统一使用页面选择的版本关联到所有导入用例

### 后端修改
- **`services.py`**: 
  - 模板 HEADERS 去除"关联版本"，SAMPLE_ROW 去除版本示例，INSTRUCTIONS 合并为"关联项目和版本"
  - HEADER_ALIASES / EXPECTED_HEADER_FIELDS 去除 versions
  - `import_record(cls, record, version_ids=list)` 新增参数，所有导入用例统一关联 version_ids
  - `_validate_row` 不再接收 version_map 参数
  - 移除 `from apps.versions.models import Version` 导入
- **`views.py`**: `TestCaseImportRecordListCreateView.create()` 接收 `version_ids` (逗号分隔)，校验是否属于所选项目后传给 Celery 任务
- **`tasks.py`**: `import_testcases_from_excel(record_id, version_ids=list)` 新增参数，透传给服务层

### 前端修改
- **`TestCaseList.vue`**:
  - 导入对话框新增版本多选下拉框（项目变更时自动加载版本，复用 `/versions/projects/{id}/versions/`）
  - 提交时添加 version_ids 校验，以逗号连接传入 FormData
- **`testcase.js`**: 新增 i18n 键 `selectImportVersions`、`selectImportVersionsPlaceholder`、`importVersionRequired`

## 测试用例新增"模块"字段

### 改动要点
- TestCase 模型新增 `module` CharField（可选，默认空字符串）
- 表格中模块列显示在序号后面（序号 → 模块 → 标题）
- 导入模板第一列增加"模块"字段
- 导出 Excel 同步增加模块列

### 后端修改
- **`models.py`**: TestCase 新增 `module = models.CharField(max_length=200, blank=True, default='', verbose_name='模块')`
- **`serializers.py`**: 
  - `TestCaseListSerializer` fields 添加 `module`
  - `TestCaseCreateSerializer` / `TestCaseUpdateSerializer` fields 添加 `module`
  - `TestCaseSerializer` 使用 `__all__` 自动包含
- **`services.py`**: 
  - 模板 HEADERS 第一列增加"模块"，SAMPLE_ROW 增加"登录模块"示例
  - INSTRUCTIONS 增加模块说明行
  - HEADER_ALIASES 增加 `'模块': 'module'`，EXPECTED_HEADER_FIELDS 第一项增加 `module`
  - `import_record()` 创建 TestCase 时包含 `module=row_data.get('module', '')`
  - column_widths 从 6 列扩展到 7 列
- **数据库迁移**: `0003_add_module_field.py`，已执行

### 前端修改
- **`TestCaseList.vue`**: 
  - 表格在序号列后新增模块列 (width=140, prop="module")
  - Excel 导出表头和数据行同步增加模块列
- **`testcase.js`**: 新增 i18n 键 `module: '模块'`、`excelModule: '模块'`

### 默认提示词同步更新
- **`docs/tester.md`**: AI 用例生成默认提示词同步新增"模块"列
  - 完整性要求新增"填写所属模块名称"
  - 输出表格格式从 8 列扩展为 9 列：`| 用例ID | 模块 | 测试目标 | 前置条件 | 操作步骤 | 预期结果 | 优先级 | 测试类型 | 关联需求 |`
  - 示例数据行新增"登录模块"

### 补充：编辑/新建/详情/评审/执行中遗漏的模块字段
1. **`TestCaseForm.vue`**: reactive form 添加 `module: ''`，模板添加模块输入框（标题前）
2. **`TestCaseEdit.vue`**: reactive form 添加 `module: ''`，fetchTestCase 提取 module
3. **`TestCaseDetail.vue`**: 详情描述中添加模块显示行
4. **`en/testcase.js`**: 补充 `module: 'Module'`、`excelModule: 'Module'`、`modulePlaceholder`
5. **`zh-cn/testcase.js`**: 补充 `modulePlaceholder: '请输入模块名称'`
6. **评审后端**: `SimpleTestCaseSerializer` 添加 `module = serializers.CharField()`
7. **评审前端**: `ReviewForm.vue` 用例选择表格添加模块列
8. **计划/执行后端**: 
   - `TestPlanDetailSerializer.get_testcases()` 添加 module
   - `TestRunCaseSimpleSerializer` 添加 `get_module()` SerializerMethodField
   - `TestRunCaseDetailSerializer` 添加 `get_module()` SerializerMethodField
9. **执行前端**: `ExecutionDetailView.vue` 用例表格添加模块列

## 用例列表列显示重排 + 筛选增强（2026-06-21）

### 主列表 TestCaseList.vue 改动
- **列重排（12列）**: 选择 → 序号 → 模块 → 用例标题 → 前置条件 → 操作步骤 → 预期结果 → 优先级 → 创建时间 → 关联项目 → 关联版本 → 操作
- **移除旧列**: 测试类型、作者（不再在表格中显示）
- **新增显示列**: 前置条件、操作步骤、预期结果（支持 HTML 渲染，`show-overflow-tooltip`）
- **筛选区新增 4 个筛选器** (共 6 个，24 span 填满):
  1. 搜索 (span 4) - 支持标题/描述/模块/步骤全文搜索
  2. 项目 (span 4) - 选中后联动加载版本列表
  3. 版本 (span 4) - 仅在选择项目后可用
  4. 模块 (span 4) - 模糊匹配输入
  5. 优先级 (span 4)
  6. 测试类型 (span 4)

### 后端 API 筛选支持 (`views.py`)
- `TestCaseListCreateView.get_queryset()` 新增:
  - `version` 参数: 按关联版本 ID 筛选 (versions__id=version)
  - `module` 参数: 按模块名模糊匹配 (module__icontains=module)
- `search_fields` 扩展为 `['title', 'description', 'module', 'preconditions', 'steps', 'expected_result']`

### 执行计划用例选择 API (`apps/executions/views.py`)
- `testcases_by_projects` 的 `.values()` 新增 `module` 字段

### ReviewForm.vue 评审用例选择对话框
- 对话框宽度从 800px → 1100px
- 新增筛选栏: 搜索/项目/版本/模块/优先级 (5个本地筛选器)
- 表格新增列: 前置条件、操作步骤、预期结果（支持 HTML 渲染）
- 移除: test_type、author 列
- 新增 `onTestcaseDialogProjectChange` 加载项目版本

### ExecutionListView.vue 执行计划用例选择对话框
- 筛选栏新增"模块"输入框
- 表格: 移除用例编号列，新增模块列
- `testcaseSelectorFilters` 新增 `module` 字段，`testcaseSelectorList` computed 新增模块匹配
- `resetTestcaseSelectorFilters` 同步重置 module

### i18n 新增
- zh-cn: `versionFilter: '版本筛选'`、`moduleFilter: '模块筛选'`、`searchPlaceholder` 扩展
- en: 对应英文翻译

## 前置条件/步骤/预期结果列显示优化 + 次要列压缩（2026-06-21）

### 改动说明
- **三大内容字段**（前置条件、操作步骤、预期结果）：去掉 `show-overflow-tooltip`，`min-width` 增大到 200px (TestCaseList) / 160px (ReviewForm 对话框)，支持多行换行显示
- **`formatRichText()`** 辅助函数：将 `\n` 转为 `<br/>`，自动识别 `1.xxx2.xxx` 模式在序号前插入 `<br/>`（匹配 `[中文/字母/标点](数字).` 边界）
- **`rich-cell` CSS**：`line-height: 1.6; word-break: break-word; white-space: normal; display: block`

### TestCaseList.vue 列宽调整
| 列 | 旧 | 新 |
|-----|-----|-----|
| 前置条件 | min-width=140 | min-width=300 (1.5x: 200→300) |
| 操作步骤 | min-width=140 | min-width=300 (1.5x: 200→300) |
| 预期结果 | min-width=140 | min-width=300 (1.5x: 200→300) |
| 优先级 | width=100 | width=80 |
| 创建时间 | width=170 | width=150 |
| 关联项目 | width=140 | width=130 |
| 关联版本 | width=180 | width=140 |

### ReviewForm.vue 对话框列宽调整
- 前置条件 min-width 120 → 210 (1.3x)，操作步骤/预期结果 min-width 120 → 240 (1.5x)

### ExecutionListView.vue 对话框列宽调整
- priority: 100→80, test_type: 140→120, project__name: 160→140

### ExecutionDetailView.vue 执行页面改造（2026-06-21）
- **后端**：`TestRunCaseSimpleSerializer` 新增 `title`/`preconditions`/`steps`/`expected_result` 字段（从关联 TestCase 获取）
- **新增三列**：前置条件(min-width=260, 1.3x) + 操作步骤(min-width=300, 1.5x) + 预期结果(min-width=300, 1.5x)，使用 `formatRichText` + `rich-cell` 换行显示
- **列名变更**："测试用例" → "用例标题"（`$t('execution.caseTitle')`）
- **去掉备注列**（之前是行内 textarea），改为操作列新增"备注"按钮，点击弹出对话框输入备注后保存
- **Locale**：zh-cn/en 新增 `caseTitle`、`addComment` 键

### ExecutionDetailView.vue 操作列新增"修改"按钮（2026-06-21）
- **后端**：`TestRunCaseViewSet` 新增 `update_testcase` action（PATCH），支持更新关联 TestCase 的 `title`/`preconditions`/`steps`/`expected_result`/`priority`
- **前端操作列**：新增"修改"按钮（type=success, icon=EditPen），排列顺序为"查看历史"→"修改"→"备注"
- **编辑对话框**：包含用例标题、前置条件、操作步骤、预期结果（均为 textarea）和优先级（select），不包含项目和迭代
- **UI 对齐**：操作列宽度 140→200，按钮使用 `.actions-cell` flex 布局（gap=6px）整洁排列
- **Locale**：zh-cn/en 新增 `editCase`（修改/Edit）、`editCaseTitle`（修改用例数据/Edit Case Data）

### 执行页面优先级相关修复（2026-06-21 12:32）
- **后端**：`TestRunCaseSimpleSerializer` 新增 `priority` 字段到序列化输出
- **列表**：模块列后新增优先级列（width=80, el-tag 居中显示），使用 `getPriorityText`/`getPriorityTagType` 中文标签（低/中/高/紧急）
- **编辑对话框**：优先级下拉选项从硬编码英文改为 `$t('testcase.low')` 等中文标签

### 执行状态列迁移到操作列（2026-06-21 12:34）
- 去除独立的"执行状态"列（el-select），状态功能整合到操作列
- 操作列新增状态下拉按钮（el-dropdown），显示当前状态中文名和对应颜色图标，点击展开可选择 5 种状态（未测试/通过/失败/阻塞/重测）
- 新增 helper 方法：`getStatusButtonType`、`getStatusButtonIcon`、`handleStatusCommand`
- 操作列宽度 200 → 260，按钮排列为 2×2 网格：`执行状态 | 历史` / `修改 | 备注`（使用 flex column + actions-row 两行布局）
- 操作列改为 icon-only 圆形按钮 + tooltip，宽度 260 → 150；preconditions min-width 260→200，steps min-width 300→240，释放空间给 expected_result
- 最总调整：仅执行状态按钮保留文字显示（具名按钮），其他三个按钮仍为圆形图标按钮；列宽 150→170；CSS 精确控制 `.is-circle` 紧凑 padding

### 添加模块筛选功能（2026-06-21 12:50）
- 在进度条/批量操作与表格之间新增筛选栏（`.filter-bar`），包含模块下拉选择器
- `getRunModules()` 从当前 run 的 run_cases 中提取去重排序的模块列表
- `getFilteredCases()` 根据 `selectedModule` 过滤用例
- 切换筛选时自动回到第1页并清空勾选
- 分页 total 同步使用过滤后的数量
- 筛选后显示结果统计文本
- locale 新增 `execution.filterByModule`、`execution.allModules`

### 评审用例页改造：项目+迭代+模块自动选择用例（2026-06-21 14:06）
- 移除原有复杂用例选择对话框（`el-dialog` + 搜索/筛选/表格勾选）
- 新流程：选择项目 → 选择迭代（可选）→ 多选模块 → 自动包含所选模块全部用例
- 每个模块选项显示用例数，底部显示总计用例数
- 迭代切换/模块切换时自动重新加载用例并更新 `form.testcases`
- 编辑模式下自动从已有用例提取模块名并回显
- 新增 `availableVersions`、`selectedVersion`、`availableModules`、`selectedModules` 状态
- 新增 `fetchVersions()`、`extractModules()`、`getModuleCaseCount()`、`onModuleChange()`、`removeModule()` 等函数
- 移除未使用的 `formatRichText`、`getPriorityText`、`searchTestcases` 及所有对话框相关代码
- locale 新增 `selectVersion`、`selectModules` 等 key

### 修复模块显示不全问题（2026-06-21 14:09）
- **问题**: 多选模块后，摘要区域模块标签过多时溢出、模块名称在标签中显示不全
- **模板修复**:
  - 模块下拉选项改用 slot 自定义渲染，左边模块名 + 右边用例数，避免一行过长被截断
  - 添加 `:max-collapse-tags="2"` 限制选择器内折叠标签数
  - `module-summary` 新增 `max-height: 200px` 限制高度，内部 `.module-tags-wrap` 可 `overflow-y: auto` 滚动
  - 标签内文字拆分 `.tag-text`（`text-overflow: ellipsis`）+ `.tag-count`（`flex-shrink: 0`），确保计数始终可见
- **CSS 修复**:
  - `.module-summary` 改为 `align-items: flex-start`（多行标签顶部对齐）
  - 全局非 scoped 样式 `.module-select-popper` 控制下拉选项为 flex 两端对齐布局
- 构建成功，无错误

### 用例评审重构：逐用例评审模式（2026-06-21 14:22）
将评审从"整体评审"模式改为"逐用例评审"模式，每个用例独立评审，操作栏模仿执行页面。

#### 后端新增
- **模型**: `ReviewCase`（中间表，review+testcase，status: unreviewed/passed/failed + comments + reviewed_by/reviewed_at）和 `ReviewCaseHistory`（评审历史）
- **序列化器**: `ReviewCaseSerializer`、`ReviewCaseListSerializer`（含关联用例字段 title/module/priority/preconditions/steps/expected_result）、`ReviewCaseHistorySerializer`
- **ViewSet**: `ReviewCaseViewSet`（/reviews/review-cases/），含 update_status（状态变更+记录历史+更新整体状态）、history（查询历史）、update_testcase（修改用例内容）三个 action
- **自动同步**: 创建/编辑评审时自动创建/同步 ReviewCase 记录
- **数据迁移**: 0003 建表 + 0004 为已有评审生成 ReviewCase 记录
- **URL**: 注册 `review-cases` basename

#### 前端新增
- **ReviewExecutionView.vue**（新页面）: 类似 ExecutionDetailView 的布局
  - 紫色渐变头部显示评审标题、状态、关联项目
  - 4张统计卡片（总数/通过/不通过/未评审）+ 进度条
  - 用例表格：序号→模块→优先级→标题→前置条件→步骤→预期结果→操作
  - 操作列：状态下拉按钮（未评审/通过/不通过）+ 历史 | 修改 + 备注（2×2 网格同执行页面）
  - 模块筛选、批量删除、分页（10/20/50/100）
  - 支持评审历史穿梭、备注编辑、用例内容修改
- **路由**: `/ai-generation/reviews/:id/execute` → `ReviewExecutionView.vue`
- **ReviewList.vue**: 操作列新增"开始评审"按钮（链接到 execute 页面），去掉旧的"评审"按钮
- **locale**: zh-cn/en 新增 `reviewExecution` 完整翻译（30+键）+ `reviewList.execute`

### 修复评审表单模块/用例数据统计不一致 & 中文显示优化（2026-06-21 14:38）

#### 问题
1. 新增/修改评审时，模块标签显示的项目总用例数与实际选中用例数不一致
2. 缺少按项目+版本快速获取模块统计的 API
3. 编辑评审时模块下拉选项计数来自全部项目用例而非版本过滤后的结果

#### 后端修改
- **`apps/testcases/views.py`**: 新增 `TestCaseModuleStatsView`（GET API）
  - 接受 `project`（必填）和 `version`（可选）参数
  - 返回 `{project_id, version_id, total_modules, total_cases, modules: [{module, count}]}`
  - 权限校验：仅能查询用户有权限的项目
- **`apps/testcases/urls.py`**: 新增 `path('module-stats/', ...)`

#### 前端修改 (ReviewForm.vue)
- **新增 `fetchModuleStats()`**: 调用 `/testcases/module-stats/` 获取精确模块统计
- **新增 `getSelectedModuleCount()`**: 标签中显示已选中用例数（从 `form.testcases` 中计数）
- **新增 `totalSelectedCases` computed**: 统一总计显示
- **新增 `getModuleStatCount()`**: 下拉选项优先从统计 API 获取计数，降级使用本地计算
- **`getModuleCaseCount()`**: 保留用于下拉选项显示可用用例总数
- 模块标签使用 `getSelectedModuleCount`（选中数），下拉选项使用 `getModuleStatCount`（API统计数）
- `onProjectChange`/`onVersionChange`/`fetchReviewData` 新增 `fetchModuleStats()` 调用
- 切换项目/版本时清空 `moduleCaseCountMap`

#### 前端修改 (ReviewExecutionView.vue)
- **新增"评审人"列**（`reviewed_by_name`），显示在预期结果列后
- 操作列宽度从 170 增加到 180（中文文本需要更多空间）
- 预期结果列 min-width 从 300 调整为 260

#### 构建状态
- 前端 `npm run build` 成功（✓ built in 12.39s）
- 后端无 linting 错误

### 修复模块标签溢出导致部分模块不可见（2026-06-21 14:47）

**问题**: API 返回 8 个模块，前端只显示 6 个，剩余模块被 `max-height: 200px; overflow: hidden` 截断不可见。

**修复** (ReviewForm.vue CSS):
- `.module-summary`: `max-height:200px` → `max-height:300px`，`overflow:hidden` → `overflow-y: auto`（支持滚动查看所有模块）
- `.module-tags-wrap`: 移除独立的 `max-height:184px` 和 `overflow-y: auto`，由父容器统一滚动
- `:max-collapse-tags="2"` → `:max-collapse-tags="10"`（选择器内显示更多已选模块标签）

**兼容性**: 修改后可支持 50 个模块（模块摘要区域最大高度 300px + 垂直滚动，足够显示大量标签）。

**构建**: ✓ built in 12.79s

### 修复模块显示不全（第二次修复 - 竞态条件根因）（2026-06-21 14:50）

**真正的根因**: `fetchTestcases()` 和 `fetchModuleStats()` 并行执行存在竞态条件。
- `fetchTestcases` 第279行 `availableModules.value = extractModules(uniqueTestcases)` 
- `fetchModuleStats` 第430行 `availableModules.value = sortedModules`（API 数据）
- 两者无 await 同步，后完成者覆盖前者。当 `fetchTestcases` 后完成时（仅含 6 个模块的测试用例数据），覆盖了 API 返回的 8 个模块。

**第二次修复**:
1. **删除 `fetchTestcases` 第279行**: 不再在该函数中设置 `availableModules`，由 `fetchModuleStats` 作为唯一权威来源
2. **降级逻辑加固**: `fetchModuleStats` 失败时仅在 `testcases.value.length > 0` 时降级到 `extractModules`
3. **下拉弹出层**: `.module-select-popper .el-select-dropdown__list { max-height: 400px }` 确保弹出层有足够高度显示所有模块选项（兼容 50 个模块）

**构建**: ✓ built in 12.72s

### 修复评审执行页面中文翻译不生效（2026-06-21 15:01）

**根因**: `review.js` 中定义了 `reviewExecution` 翻译键（zh-cn 33个键 + en 33个键），但语言索引文件漏掉了合并。

**修复**（两处）:
- `frontend/src/locales/lang/zh-cn/index.js`: 新增 `reviewExecution: reviewModule.reviewExecution,`
- `frontend/src/locales/lang/en/index.js`: 新增 `reviewExecution: reviewModule.reviewExecution,`

**影响**: 评审执行页面所有 `$t('reviewExecution.xxx')` 调用现在能正确解析为中文/英文文本：
| 键 | 中文 | 英文 |
|-----|------|------|
| serialNumber | 序号 | # |
| caseTitle | 用例标题 | Case Title |
| reviewedBy | 评审人 | Reviewer |
| actions | 操作 | Actions |
| 等 33 个键 | 全部恢复 | 全部恢复 |

**构建**: ✓ built in 12.14s

### 重构：评审保存模块名而非用例ID列表（2026-06-21 15:06）

**根因**: 保存评审时将已加载用例ID（API分页仅100条）传给后端，导致选中模块有200个用例但只保存了100个。

**架构变更**: 取消按用例ID保存，改为只保存选中的模块名，后端按模块名查询所有用例。

#### 后端修改
- **Model** (`apps/reviews/models.py`): `TestCaseReview` 新增 `modules = JSONField(default=list, blank=True)` 字段
- **Migration**: `0005_add_review_modules.py` 自动生成并应用
- **Serializer** (`apps/reviews/serializers.py`):
  - `TestCaseReviewCreateSerializer`: `testcases` (ListField(IntegerField)) → `modules` (ListField(CharField))
  - 新增 `_resolve_testcase_ids(modules, project_ids)`: 按模块名+项目查询全部用例ID
  - `create()`/`update()`: 从 modules 自动填充 testcases M2M 和 ReviewCase 记录
  - `TestCaseReviewSerializer`: 响应中新增 `modules` 字段
- **View** (`apps/reviews/views.py`): 新增 `sync_review_cases` action（POST），执行时动态刷新模块下所有用例：
  - 查询模块下所有用例ID → 更新 `review.testcases`
  - 新增/删除 ReviewCase 记录以匹配当前模块用例列表

#### 前端修改
- **ReviewForm.vue**:
  - `saveReview()`: 发送 `modules: selectedModules.value` 而非 `testcases: form.testcases`
  - `onModuleChange()`: 不再将用例ID填入 form.testcases
  - `totalSelectedCases`/`getSelectedModuleCount`: 从 `moduleCaseCountMap`（API统计）计算，不再依赖已加载用例数
  - `fetchReviewData()`: 编辑时从 `review.modules` 回填 selectedModules，兼容旧数据降级到 extractModules
  - 移除 `form.testcases` 验证规则，改为 save 前手动校验 `selectedModules`
  - 移除 `onProjectChange`/`onVersionChange` 中的 `form.testcases = []`
- **ReviewExecutionView.vue**:
  - `fetchReview()`: 加载评审前调用 `sync_review_cases` 确保模块下所有用例都有 ReviewCase 记录

#### 数据流对比
| 阶段 | 修改前 | 修改后 |
|------|--------|--------|
| 模块选择 | 选中模块 → 查已加载用例ID → 存入 form.testcases | 选中模块 → selectedModules |
| 保存 | 发送 testcases (用例ID列表, 最多100) | 发送 modules (模块名列表) |
| 后端处理 | 直接保存用例ID | 按模块名查询全部用例ID → 保存 |
| 执行 | 加载已保存的 ReviewCase | sync_review_cases → 加载 ReviewCase |

**构建**: ✓ built in 12.91s

### 修复评审执行页面分页截断 + 批量状态更新（2026-06-21 15:15）

**根因**: `ReviewCaseViewSet` 继承 DRF 全局分页 `PageNumberPagination`（PAGE_SIZE=20），导致171条用例只返回20条，模块筛选和统计均基于截断数据。

#### 后端修改
- **`apps/reviews/views.py`** - `ReviewCaseViewSet`:
  - 新增 `pagination_class = None` 禁用分页，前端自行分页（已实现 el-pagination）
  - 新增 `batch_update_status` action（POST `/reviews/review-cases/batch_update_status/`）:
    - 接收 `ids[]` + `status` + `comments`
    - 批量更新 ReviewCase 状态、记录 ReviewCaseHistory
    - 更新关联 Review 整体状态

#### 前端修改
- **`ReviewExecutionView.vue` - 批量操作栏**:
  - 新增批量状态下拉按钮（通过/不通过/未评审），带确认弹窗
  - `isBatchUpdating` 状态控制加载中
  - `handleBatchStatusCommand()` 调用批处理API，成功后更新本地数据并清空选择
  - 新增 `.batch-count` 样式显示已选数量
- **i18n** (`zh-cn/review.js` + `en/review.js`):
  - 新增 `batchUpdateStatus`、`batchSetPassed`、`batchSetFailed`、`batchSetUnreviewed`
  - 新增 `batchSelected`、`batchStatusConfirm`、`batchStatusSuccess`、`batchStatusFailed`

**构建**: ✓ built in 12.19s

### 测试计划执行页面新增批量状态更新功能（2026-06-21 15:19）

**需求**: 测试计划执行页面（ExecutionDetailView）缺少批量状态更新功能，需要类似评审执行页面的批量通过/失败/未测试等功能。

#### 后端修改
- **`apps/executions/views.py`** - `TestRunCaseViewSet`:
  - 新增 `batch_update_status` action（POST `/executions/run_cases/batch_update_status/`）:
    - 接收 `ids[]` + `status` + `comments`
    - 批量更新 TestRunCase 状态（untested/passed/failed/blocked/retest）
    - 记录 TestRunCaseHistory
    - 批量更新关联 TestRun 的整体状态
  - 新增 `_update_run_status(test_run)`: 根据 progress_stats 自动计算 TestRun 状态（untested/in_progress/completed）
  - 现存 `update_status` 单个更新也调用 `_update_run_status` 保持一致性

#### 前端修改
- **`ExecutionDetailView.vue` - 批量操作栏**:
  - 新增批量状态下拉按钮（未测试/通过/失败/阻塞/重测），带确认弹窗
  - `isBatchUpdating` 状态控制加载中
  - `handleBatchStatusCommand()` 调用 `POST /executions/run_cases/batch_update_status/`
  - 成功后更新本地数据 + 清空选择 + 刷新测试计划数据更新进度统计
  - 新增 `.batch-count` 样式显示已选数量

#### i18n 翻译
- **`zh-cn/execution.js`**: 新增 `batchUpdateStatus`(批量状态), `batchSelected`(已选), `batchStatusConfirm`, `batchStatusSuccess`, `batchStatusFailed`
- **`en/execution.js`**: 新增对应英文翻译

**构建**: ✓ built in 13.30s

### 优化批量状态更新接口性能（2026-06-21 15:24）

**问题**: 传入100个用例ID时，`batch_update_status` 接口响应慢。原因：逐条循环 save() + create() 产生 200+ SQL。

#### 优化方案（bulk_create + queryset.update + 聚合统计）

**SQL 数量对比（100条用例）：**

| 操作 | 优化前 | 优化后 |
|------|--------|--------|
| ReviewCase/TestRunCase 更新 | 100次 save() | 1次 update() |
| History 记录 | 100次 create() | 1次 bulk_create() |
| Review/TestRun 状态更新 | N*3次 count() + N次 save() | 1次 annotate聚合 + 1次 in_bulk + 1次 bulk_update |
| **总计** | **~300+ SQL** | **~6 SQL** |

#### 后端修改

- **`apps/reviews/views.py`**:
  - `batch_update_status`: 用 `list(qs.values('id','review_id'))` 先取数据 → `bulk_create` 批量创建历史 → `queryset.update()` 批量更新状态
  - 新增 `_batch_update_review_statuses(review_ids)`: 一次 annotate 聚合统计 + `in_bulk` 加载评审 + `bulk_update` 批量更新
  - `_update_review_status` (单个): 新增 `review.completed_at = None` 当未全部评审时清除完成时间
  - 导入新增 `Count`

- **`apps/executions/views.py`**:
  - `batch_update_status`: 同样的优化模式（bulk_create + update + 聚合统计）
  - 新增 `_batch_update_run_statuses(run_ids)`: 一次 annotate 聚合统计 + `in_bulk` + `bulk_update`
  - `_update_run_status` (单个): 保持不变
  - 导入新增 `Count, Q`

**构建/验证**: ✓ 两个 ViewSet 导入成功，无 lint 错误

### 修复批量状态更新"失败通知但实际成功"的 Bug（2026-06-21 15:30）

**现象**: 前端弹出"批量状态更新失败"警告，但后端实际已更新成功。

**根因**: `ExecutionDetailView.vue` 的 `handleBatchStatusCommand` 中，`fetchTestPlan()` 调用放在和 `api.post()` 同一个 try-catch 块内。批量更新 API 成功后，`fetchTestPlan()` 如果失败会进入同一个 catch，显示误导性的"批量状态更新失败"消息。

**修复** (`ExecutionDetailView.vue`):
- 将 `await fetchTestPlan()` 从主 try-catch 块中移出，放入独立的 try-catch
- 刷新失败不再影响批量更新成功提示

**构建**: ✓ built in 12.21s

### 修复 v-for 中 ref 导致 clearSelection 报错（2026-06-21 15:34）

**现象**: 数据量大时批量修改状态报错 `TypeError: C.value.clearSelection is not a function`，控制台报错链 `handleSizeChange → clearSelection`。数据量少不报错是因为不需要触发分页 size 变化。

**根因**: `ref="tableRef"` 定义在 `v-for` 循环内的 `<el-table>` 上。Vue 3 中 `v-for` 内的 ref 会变成数组，而数组没有 `clearSelection()` 方法。当数据量多时触发分页/筛选等操作调用 `tableRef.value.clearSelection()` 就报错。

**修复** (`ExecutionDetailView.vue`):
- 新增 `clearTableSelection()` 辅助函数，兼容数组（v-for）和单元素两种 ref 类型
- 替换 4 处 `tableRef.value.clearSelection()` 调用：batch status update、handleModuleFilterChange、handlePageChange、handleSizeChange

**构建**: ✓ built in 13.39s
