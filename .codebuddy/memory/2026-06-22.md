# 2026-06-22

## 修复 App 自动化设备截图 500 错误

**现象**: `POST /api/app-automation/devices/1/screenshot/` 返回 500，前端控制台报 `Failed to load resource: 500` 和 `截图失败`

**根因** (`apps/app_automation/views/device_views.py` screenshot 方法):
1. `subprocess.CalledProcessError` 未单独捕获 — ADB 命令失败时被笼统的 `except Exception` 吞掉，丢失 stderr 错误详情
2. `FileNotFoundError` 未单独捕获 — ADB 路径无效时无法给出明确提示
3. 远程设备（`remote_emulator`）未处理 `device_id` 格式（IP:port）
4. 缺少 Windows `CREATE_NO_WINDOW` 参数

**修复** (后端):
- 新增 `platform` 导入
- 按优先级捕获异常: `FileNotFoundError` → `CalledProcessError`（含 stderr） → `TimeoutExpired` → `Exception`
- 远程设备自动补全 `device_id` 为 `IP:port` 格式
- Windows 添加 `CREATE_NO_WINDOW`
- 日志记录返回码和 stderr

**修复** (前端 `CaptureElementDialog.vue`):
- catch 块展示后端返回的具体错误消息: `error?.response?.data?.msg`

**构建**: ✓ built in 17.04s

---

## 测试用例模块筛选改为下拉框 (TestCaseList.vue)

**需求**: 模块搜索从输入框改为下拉框，下拉数据来源于当前迭代（版本）的模块，未选择项目/迭代时置灰。

**修改内容**:
- **模板**: `el-input` → `el-select`，`:disabled="!projectFilter"`，选项显示 `模块名 (用例数)`
- **新增** `moduleOptions = ref([])` 变量
- **新增** `fetchModuleOptions(projectId, versionId)`: 调用 `GET /testcases/module-stats/?project=X&version=Y` 加载模块列表
- **新增** `onVersionFilterChange(versionId)`: 版本切换时清空模块筛选并重新加载模块选项
- **修改** `onProjectFilterChange`: 新增清空 `moduleFilter` 和 `moduleOptions`，选中项目时加载模块
- **修改** version select: `@change="handleFilter"` → `@change="onVersionFilterChange"`（需要额外触发模块下拉刷新）
- 复用已有 API: `/testcases/module-stats/`

**构建**: ✓ built in 22.08s

---

## 修复: UI层级定位点击截图无法自动填充定位值

**现象**: 在"从设备截图创建元素"对话框中切换为"UI层级定位"类型截图后，点击覆盖层上的元素矩形框，`定位值`输入框不会自动填充。

**根因** (`CaptureElementDialog.vue`):
- `.hierarchy-element-rect` 上的 `@click.stop` 只阻止了 `click` 事件冒泡，未阻止 `mousedown` 冒泡
- `mousedown` 冒泡到 `.image-wrapper` 触发 `handleMouseDown`，创建零大小的 `.selection-box` 选区框
- `.selection-box` 在 DOM 中位于 `.hierarchy-overlay` 之后，无 z-index 设置，默认覆盖在上方
- 选区框遮挡了 `mouseup` 和 `click` 事件，导致 `selectUIElement` 永远不被调用

**修复**: 在 `.hierarchy-element-rect` 上添加 `@mousedown.stop` 阻止 mousedown 冒泡

**构建**: ✓ built in 18.77s

---

## 修复: UI层级定位4个问题

**现象**:
1. 第一次截图时切换到UI层级定位不生效
2. 后续加载层级信息比较慢（重复截图）
3. 元素定位方式手动选择其他类型后，点击图片元素又变回 resource_id
4. 图层下方元素矩形没有显示全（overflow hidden 裁剪）

**修复**:

### 后端 `device_views.py`
- `dump_hierarchy` 新增 `skip_screenshot` URL 参数：前端已有截图时跳过 ADB 截图（节省 ~3-5s）
- 跳过截图时只获取分辨率和 dump 层级 XML

### 前端 API `app-automation.js`
- `dumpDeviceHierarchy(id, { skipScreenshot })` 支持 options 参数

### 前端 `CaptureElementDialog.vue`
1. **切换不生效**: 新增 `watch(formData.element_type)`，切换到 uiautomator 且已有截图时自动调用 `loadHierarchy()`
2. **加载慢**: `loadHierarchy()` 传入 `skipScreenshot: !!capturedImage.value`，避免重复 ADB 截图
3. **定位方式被覆盖**: 新增 `userSelectedLocatorType` ref 追踪手动选择；`selectUIElement()` 优先使用用户选择的定位类型填充值，找不到才回退自动选择；切换元素类型或关闭对话框时重置标记
4. **底部元素不全**: `.capture-left` 的 `overflow: hidden` 改为 `overflow: auto`，`align-items: center` 改为 `flex-start`，支持垂直滚动

**构建**: ✓ built in 18.70s

---

## 修复: uiautomator dump 退出码 137 (OOM Kill)

**现象**: `adb shell uiautomator dump /data/local/tmp/testhub_uidump.xml` 返回退出码 137
**根因**: 退出码 137 = 128 + 9 (SIGKILL)，设备 OOM Killer 杀死 uiautomator 进程

**修复** (`device_views.py` dump_hierarchy):
- **多路径容错**: 依次尝试 `/data/local/tmp/` → `/sdcard/` 两个路径 dump
- **预清理**: 每次 dump 前 `rm -f` 清理设备上残留文件
- **超时增加**: 10s → 15s
- **成功判断**: 检查 stdout 中是否包含 "dumped to" 确认 dump 成功
- **错误区分**: 退出码 137 给出明确提示"设备内存不足，建议关闭后台应用"；区分 "not found" 等错误
- `CalledProcessError` 处理器同样增加 137 友好提示

---

## 修复: 定位方式不生效 + 元素信息显示不全

**现象**:
1. 手动选择定位方式（如 text）后点击元素，定位方式和值会被覆盖回 resource_id
2. 选中元素后信息卡片只显示少量字段，缺少 content_desc/xpath/checkable/scrollable，长文本溢出

**修复** (`CaptureElementDialog.vue`):

### 问题1: 定位方式被覆盖
- **根因**: `watch(() => formData.uiautomator.locator_type)` 在 `autoSelectLocatorType()` 程序式修改类型时也会触发，错误标记为用户手动选择；且 `selectUIElement()` 在用户所选类型无匹配值时回退到 `autoSelectLocatorType` 覆盖用户选择
- **修复**:
  - 删除 `watch(locator_type)`，改用 el-select 的 `@change="onUserSelectLocatorType"` 事件（仅用户操作触发）
  - 新增 `setLocatorTypeProgrammatic(type, value)` 函数，程序式设置前先清除 `userSelectedLocatorType` 标记
  - `autoSelectLocatorType()` 改用 `setLocatorTypeProgrammatic()` 避免污染用户标记
  - `selectUIElement()` 中元素缺少用户所选类型的值时，保留类型并给出 warning 提示，不再回退覆盖

### 问题2: 元素信息不全
- **修复**:
  - 扩展信息卡片：增加 class_name_full、content_desc、xpath、package、checkable/scrollable 标签、depth/index
  - 布局改为分行标签+值，xpath 使用等宽字体灰色底色
  - 卡片 max-height: 260px + overflow-y: auto 支持滚动
  - 值添加 `word-break: break-all; overflow-wrap: break-word` 防止长文本溢出

**构建**: ✓ built in 12.43s, CaptureElementDialog chunk: 26.32 kB

---

## 修复: XPath 开头异常 `/[1]/` 

**现象**: UI 层级元素的 XPath 以 `/[1]/` 开头（缺少类名前缀），如:
`/[1]/android.widget.FrameLayout[1]/...RecyclerView[@resource-id='...']`
正常应为: `/FrameLayout[1]/...RecyclerView[@resource-id='...']`（或其他有效格式）

**根因**: XML 根节点 `<hierarchy>` 没有 `class` 属性，`class_name=''`，导致 `child_path = f"{''}/{[}1]"` → `"/[1]"` 污染所有子元素 XPath

**修复** (`device_views.py` L533-537): 调用 `_parse_hierarchy_nodes()` 时跳过 `<hierarchy>` 根包装节点，直接迭代子节点:
```python
elements = []
for child in root:
    elements.extend(self._parse_hierarchy_nodes(child))
```

---

## 修复: 层级叠加层无text文本 + 截图显示不完整 + 底部元素缺失

**现象**:
1. 层级叠加层只显示彩色矩形框，没有 text 文本标签，难以识别元素
2. 截图显示不完整，底部内容被截断需要滚动
3. 底部 UI 层级元素看不见

**修复** (`CaptureElementDialog.vue`):

### 问题1: 无text标签
- 在 `.hierarchy-element-rect` 内新增 `.hierarchy-text-label`，当 `elem.text` 非空时始终显示蓝色背景文本标签
- 选中的元素标签变为深蓝色加粗
- 悬停 tooltip 增加 `content_desc` 回退和 `[可点击]` 标记

### 问题2+3: 截图被截断
- 对话框 `top`: 4vh → 2vh，`width`: 94vw → 96vw (更多显示空间)
- 容器高度: `calc(100vh - 200px)` → `calc(100vh - 150px)` (多 50px)
- 图片 `max-height`: `calc(100vh - 220px)` → `calc(100vh - 170px)` (多 50px)
- 移除 `.image-wrapper` 的 `max-height: 100%` 限制
- 移除 `.capture-image` 的 `object-fit: contain`（img 默认已等比缩放）

### CSS 新增
- `.hierarchy-text-label`: 绝对定位在矩形上方，蓝底白字 11px，max-width 250px + ellipsis
- `.hierarchy-text-selected`: 选中状态深蓝加粗
- `.hierarchy-element-rect` 添加 `overflow: visible` 允许标签溢出
- `.hierarchy-tooltip` 优化样式

**构建**: ✓ built in 11.47s, CaptureElementDialog chunk: 26.56 kB

---

## 修复: 二次点击属性错误 + 每次点击刷新属性 + 截图底部截断

**现象**:
1. 第一次点击UI层级元素属性正常，第二次及以后定位方式不自动切换，属性显示错误
2. 每次点击UI层级元素需要获取最新的属性（包括 text 值）
3. 截图底部仍然显示不全

### Bug根因（问题1）

`autoSelectLocatorType()` → `setLocatorTypeProgrammatic()` 设置 `formData.uiautomator.locator_type` 后，`<el-select>` 的 `@change` 事件被 Vue 响应式触发，调用 `onUserSelectLocatorType()` → `userSelectedLocatorType.value = true`。导致第二次点击时 no longer auto-detects best locator type，而是沿用第一次的定位方式查值。

### 修复 (`CaptureElementDialog.vue`):

**问题1: 程序式修改被 @change 覆盖**
- 新增 `programmaticLocatorChange` 标志位
- `setLocatorTypeProgrammatic()`: 设置前 `programmaticLocatorChange = true`，`nextTick` 后恢复
- `onUserSelectLocatorType()`: 如果 `programmaticLocatorChange` 为 true 则忽略

**问题2: 每次点击刷新最新属性**
- `selectUIElement()` 改为 async，先立即显示缓存数据，然后后台调用 `dumpDeviceHierarchy()` 获取最新层级
- 在最新层级中通过 bounds/resource-id/xpath 三重匹配找到同一元素
- 匹配成功后用最新数据重新选中，更新 `hierarchyElements` 列表
- 新增 `applyElementToForm(elem)` 提取为独立函数避免重复代码
- 右侧面板新增 `refreshingElement` 状态的"刷新属性中..."标签提示

**问题3: 截图底部截断**
- 容器高度: `calc(100vh - 150px)` → `calc(100vh - 170px)`
- 图片 max-height: `calc(100vh - 170px)` → `calc(100vh - 200px)`
- 新增 `:deep(.el-dialog__body) { max-height: calc(100vh - 130px); overflow: hidden; }` 扩大可用空间

### 新增 import
- `nextTick` from vue
- `Loading` from @element-plus/icons-vue

**构建**: ✓ built in 16.35s, CaptureElementDialog chunk: 27.69 kB

---

## 修复: 刷新后text不显示 + 截图底部元素无法点击

**现象**:
1. 后端返回 `"text": "退出登录"` 但前端不显示 text（第一次正常，第二次及以后异常）
2. 描述更正: 不是截图显示不全，而是选择UI层级定位后，截图**下方**的UI元素无法点击选择

### Bug根因

**问题1: text不显示**
- `selectUIElement()` 刷新时通过 bounds 匹配元素 (`Math.abs < 5px`)，会用 `Array.find()` 返回第一个匹配项
- 同一 bounds 可能有多个重叠元素（父容器 + 子 TextView），find() 返回的第一个元素可能是没有 text 的父容器
- 父容器覆盖了 `selectedUIElement`，导致 `v-if="selectedUIElement.text"` 为 false，text 消失

**问题2: 底部无法点击**
- `el-dialog__body` 设置 `overflow: hidden`，阻止了 `.capture-left` 的内部滚动
- `.capture-container` 固定高度与 dialog body 的 `max-height` 不匹配（差40px），导致容器被截断
- 图片底部区域的 hierarchy 覆盖层矩形超出可见范围，无法被点击

### 修复 (`CaptureElementDialog.vue`):

**问题1: 属性丰富度优先匹配**
- 新增 `calcAttrScore(fe)`: 按 resource_id(100) > text(50) > content_desc(30) > clickable(20) > checkable(10) 计算丰富度分数
- bounds 匹配改为收集所有候选元素，然后按分数排序取最高分
- 新增 xpath 精确匹配和 text 内容匹配作为兜底
- 这样即使同一 bounds 有多个元素，优先返回 text 属性最丰富的那个

**问题2: CSS 布局重构**
- `el-dialog__body`: 改为固定 `height: calc(100vh - 145px)` + `display: flex; flex-direction: column`
- `.capture-container`: `height` → `flex: 1; min-height: 0`（自适应填充）
- `.capture-image`: 移除 `max-height` 限制，由 `.capture-left` 的 `overflow: auto` 处理滚动
- 这样图片完整显示，底部元素可通过滚动访问

**构建**: ✓ built in 12.31s, CaptureElementDialog chunk: 28.02 kB

---

## 修复: calcAttrScore 评分导致 text 不显示（第三次修复）

**现象**: 前两次修复仍然无效，text 属性始终不显示

**根因**:
`calcAttrScore` 评分 `resource_id`(100) > `text`(50)，导致同一 bounds 下：
- 父容器 LinearLayout: resource_id + clickable = 120 分
- 子 TextView: text="退出登录" = 50 分

父容器得分更高被选中，覆盖了有 text 的子元素，`selectedUIElement.text` 为空 → `v-if="selectedUIElement.text"` 不渲染

**修复** (`CaptureElementDialog.vue`):

### 匹配策略重构
1. **`calcMatchScore(fe, originalElem)` 替代 `calcAttrScore(fe)`**:
   - text: 300 基础分（最高权重）
   - text 与原始元素匹配: +500（关键：确保保留了原始 text 的元素不丢失）
   - resource_id: 100 基础分
   - resource_id 与原始匹配: +200
   - class_name 与原始匹配: +150
   - 原则：text 是最重要的用户可见属性

2. **5 级匹配优先级**:
   - ① 精确 xpath 匹配（最先，最可靠）
   - ② bounds + class_name 精确匹配（防止父容器干扰）
   - ③ bounds 匹配 + 候选排序（带原始元素参考）
   - ④ resource_id 精确匹配
   - ⑤ 全元素 text 精确匹配（兜底）

**构建**: ✓ built in 11.34s, CaptureElementDialog chunk: 28.50 kB

---

## 第四次修复: text/content_desc 字段始终显示，无值显示"(无)"

**用户反馈**: "元素信息必须有text，没有text显示无"

**根因**: 模板中 `v-if="selectedUIElement.text"` 和 `v-if="selectedUIElement.content_desc"` 导致元素没有 text 时整行隐藏，用户看不到 text 字段

**修复** (`CaptureElementDialog.vue` 第 269-272 行):
- text 行: 移除 `v-if`，值改为 `{{ selectedUIElement.text || '(无)' }}`
- content_desc 行: 同样移除 `v-if`，值改为 `{{ selectedUIElement.content_desc || '(无)' }}`
- 行为与 resource-id 行一致（参考第 266 行 `{{ selectedUIElement.resource_id || '(无)' }}`）

**构建**: ✓ built in 11.30s, CaptureElementDialog chunk: 28.39 kB

---

## 第五次修复: 异步刷新覆盖已正确选中元素导致 text 不显示（彻底修复）

**用户反馈**: "明明有text就是不显示" — API 返回数据包含 text 字段但 UI 不显示

**根因**: `selectUIElement` 的异步刷新逻辑是真正的罪魁祸首:
1. 用户点击有 text 的元素 → `selectedUIElement = elem` ✅ 正确显示
2. 异步刷新 `dumpDeviceHierarchy` 返回新数据
3. 匹配逻辑找到**不同元素**（父容器，无 text）
4. `selectedUIElement = freshElem` → **覆盖为无 text 的元素** → 显示 "(无)"

**修复** (`selectUIElement` 第 643-720 行):
- **核心变更**: 如果点击的元素已有 text，直接 return，跳过异步刷新
- 只在元素无 text 时才刷新尝试获取
- 刷新后仅在找到的元素有 text 时才更新 `selectedUIElement`
- 删除了不再使用的 `calcMatchScore` 函数
- 添加 `console.log` 调试语句方便排查

**构建**: ✓ built in 11.74s, CaptureElementDialog chunk: 28.35 kB (JDvv4yJM)

---

### 第六次修复 - Hierarchy 叠加层底部覆盖不全 (2026-06-22 13:34)

**问题**: 用户反馈截图下方区域无法点击选择元素，hierarchy 元素矩形没有覆盖到图片底部

**分析**:
- `.capture-left` 使用了 `display: flex; align-items: flex-start; justify-content: center;`
- Flex 容器内 `inline-block` 的 `.image-wrapper` 高度可能被 flex 约束，导致 hierarchy overlay 无法覆盖图片完整高度
- 当图片高度超过容器可见区域时，底部元素无法被点击

**修复** (CaptureElementDialog.vue 样式部分):
1. `.capture-left`: 移除 `display: flex; align-items: flex-start; justify-content: center;`，改用 `text-align: center;`（普通 block 容器 + inline-block 居中）
2. `.image-wrapper`: 移除 `flex-shrink: 0`（不再在 flex 容器内），添加 `vertical-align: top` 确保从顶部对齐
3. `.capture-image`: 添加 `height: auto; flex-shrink: 0;` 保留
4. `.hierarchy-overlay`: 使用 `top/left/right/bottom: 0` 替代 `width/height: 100%`，确保完全覆盖
5. `.empty-state`: 添加 `min-height: 300px`

**构建**: ✓ built in 11.57s, CaptureElementDialog chunk: 28.35 kB (CMorYiLV)

---

### 第七次修复 - 第二次点击 text 丢失 (2026-06-22 13:37)

**问题**: 用户反馈"第一次点击就有text属性，第二次点击就没有了"

**根因分析**:
`selectUIElement` 函数的逻辑缺陷：

1. 第一次点击元素 A（有 text）→ `if (elem.text)` 命中，直接 return ✅
2. 第二次点击元素 B（无 text）→ 触发异步刷新 → `hierarchyElements.value = freshElements` **无条件覆盖了整个层级列表** ❌
3. 此时再点回元素 A → 列表已被替换为刷新后的数据，而刷新后元素 A 可能丢失了 text → 显示 "(无)"

核心 bug：第 679 行 `hierarchyElements.value = freshElements` 总是无条件执行，无论刷新结果好坏都会替换原有数据。

**修复**: 简化 `selectUIElement` 逻辑：
- 移除了所有 console.log 调试语句
- **只在刷新后找到了有 text 的匹配元素时**，才更新 `hierarchyElements` 和 `selectedUIElement`
- 如果刷新后仍然没有 text，保持原有数据不变
- 删除了无用的 else 分支和 ElMessage 警告

**构建**: ✓ built in 12.40s, CaptureElementDialog chunk: 27.52 kB (DibU7lXC)

---

### 第八次修复 - 底部元素仍无法选择 (2026-06-22 13:38)

**问题**: 用户反馈底部("退出登录")区域仍然没有蓝色矩形覆盖，无法选择元素

**根因分析**:
- CSS `inline-block` 在某些浏览器/场景下高度计算不稳定
- `getHierarchyRectStyle` 没有对 bounds 进行 clamping，如果元素 bounds 超出图片边界会导致渲染异常
- hierarchy 矩形在底部可能高度太小无法点击

**修复** (CaptureElementDialog.vue):

1. **`getHierarchyRectStyle` 增强**:
   - 对 bounds 坐标 clamp 到 `[0, imgWidth]` / `[0, imgHeight]` 范围内
   - 宽度/高度 ≤ 0 时返回 `display: none`
   - 最小高度设为 8px 保证可点击

2. **`.image-wrapper` CSS **:
   - `display: inline-block` → `display: block; width: fit-content; margin: 0 auto`
   - `fit-content` 让 wrapper 精确包裹图片尺寸，`margin: 0 auto` 居中

3. **`.capture-left` CSS**: 移除无用的 `text-align: center`

4. **`.capture-image` CSS**: 添加 `pointer-events: none` 让点击穿透图片到 hierarchy 元素

5. **`handleImageLoad`**: 添加强制设置 `minHeight` 确保 overlay 尺寸正确

**构建**: ✓ built in 15.42s, CaptureElementDialog chunk: 27.75 kB (C4nN1y9z)

---

### 第九次修复 - 第一次点击元素就没有 text 了 (2026-06-22 13:43)

**问题**: 第七次修复矫枉过正 — 只有刷新后找到有 text 的匹配元素时才更新 `hierarchyElements`。导致第一次点击无 text 元素时列表不更新，第二次点击其他元素时也无法匹配。

**修复**: 改为**始终用最新数据更新 `hierarchyElements`**，同时更新 `selectedUIElement` 为刷新后的匹配元素（无论有无 text）：

```js
// 始终用最新数据更新层级列表
hierarchyElements.value = freshElements

if (freshElem && freshElem.text && freshElem.text.trim()) {
  // 有 text 的匹配元素，更新选中
  selectedUIElement.value = freshElem
  applyElementToForm(freshElem)
} else if (freshElem) {
  // 无 text 也更新（bounds 等可能不同）
  selectedUIElement.value = freshElem
  applyElementToForm(freshElem)
}
```

**构建**: ✓ built in 11.49s, CaptureElementDialog chunk: 27.75 kB (DrIJ2LvT)

---

### 第十次修复 - 500错误 + 集成 uiautomator2 解决 text 丢失 (2026-06-22 15:47)

**问题1**: `skip_screenshot=1` 时 `adb pull` 报500错误：`failed to stat remote object '/data/local/tmp/testhub_uidump.xml'`

**问题2**: text 属性无法正常显示（`adb uiautomator dump` 对很多元素无法获取 text）

**根因**:
- `adb shell uiautomator dump` 先写文件再 pull，存在文件路径权限/异步写入问题
- `adb uiautomator dump` 对 text 属性的获取能力有限（仅通过 accessibility）
- weditor 使用 `uiautomator2` 库，通过 `adb shell uiautomator dump /dev/tty` 直接输出到 stdout，不经过文件，且元素属性更丰富

**修复** (device_views.py):

1. **新增 uiautomator2 导入**: `U2_AVAILABLE` 标志控制

2. **新增 `_get_u2_device()`**: 根据连接类型建立 uiautomator2 设备连接
   - USB: `u2.connect(device_id)`
   - 远程: `u2.connect(ip:port)`

3. **新增 `_dump_hierarchy_with_u2()`**: 
   - 使用 `d.dump_hierarchy()` 直接获取 XML（通过 stdout，无文件操作）
   - 不会出现路径/权限问题

4. **新增 `_dump_hierarchy_with_adb()`**: 重构为独立回退方法
   - dump 后先用 `ls -l` 验证文件存在再确认成功
   - 修复了"声称成功但文件不存在"的假阳性问题

5. **修改 `dump_hierarchy` 主流程**:
   - 优先使用 uiautomator2（获取更丰富的 text 属性）
   - 失败时自动回退到 adb dump
   - u2 还能提供更准确的屏幕分辨率

**构建**: ✓ backend 编译通过，frontend built in 18.21s

---

### 第十一次修复 - 500错误 OOM(137) + uiautomator2 连接优化 (2026-06-22 15:54)

**问题**: `skip_screenshot=1` 时仍然 500 错误 "返回码137：uiautomator 进程被设备杀死"

**根因**:
- uiautomator2 路径失败（设备可能没有 ATX agent 或连接失败），静默回退到 adb
- adb `uiautomator dump` 因设备内存不足被 OOM killer 杀死（返回码 137）
- 即使释放内存后重试也会失败

**修复** (device_views.py):

1. **`_get_u2_device`**: 保持不变（u2.connect 本身会尝试启动 agent）

2. **新增 `_ensure_u2_atx_agent()`**:
   - 检测 ATX agent 是否已运行
   - 未运行时调用 `d.reset_uiautomator()` 重新启动
   - 带超时和验证

3. **`_dump_hierarchy_with_u2` 增强**:
   - 连接成功后验证 ATX agent 可用
   - 连接失败时记录详细原因
   - dump 成功时记录 XML 大小

4. **新增 `_free_device_memory()`**:
   - dump 前按 Home 键最小化前台应用（减少 UI 树复杂度）
   - 杀掉已知可安全关闭的非系统应用（Chrome 等）
   - 等待内存回收

5. **`_dump_hierarchy_with_adb` 重构为 OOM 重试机制**:
   - 提取 `attempt_dump()` 内联函数避免代码重复
   - 首次 dump 失败且 rc=137 时，调用 `_free_device_memory()` 释放内存
   - 等待 2s 后自动重试第 2 次
   - timeout 从 15s 增加到 20s
   - 错误信息更详细（标注尝试次数）

**工作流程**:
```
uiautomator2 连接 → 安装/启动 ATX agent → dump_hierarchy()
       ↓ 失败                                    ↓ 成功
adb uiautomator dump (第1次)              → 返回 XML
       ↓ rc=137 (OOM)
释放内存 + 等待 2s
       ↓
adb uiautomator dump (第2次-释放内存后)
       ↓ 还是失败 → 友好错误提示
```

**构建**: ✓ backend 编译通过，frontend built in 18.18s

---

### 第十二次修复 - 去掉每次点击元素的重复请求 (2026-06-22 16:01)

**问题**: 用户反馈后端已经有 text 数据，但前端每次选择元素都要重复请求后端，且 text 属性获取不到。

**根因**: `selectUIElement` 函数在元素没有 `text` 时会自动调用 `dumpDeviceHierarchy` 重新请求后端。这完全没有必要——后端在 `loadHierarchy` 时一次性返回了所有元素数据，每个元素已经包含 text。重复请求不会让没 text 的元素突然有 text。

**修复** (CaptureElementDialog.vue):

1. **`selectUIElement` 简化为纯同步函数**:
   ```js
   const selectUIElement = (elem) => {
     selectedUIElement.value = elem
     applyElementToForm(elem)
   }
   ```
   - 去掉了 60+ 行的异步刷新逻辑
   - 不再调用 `dumpDeviceHierarchy`
   - `refreshingElement` 相关逻辑一并移除

2. **模板清理**: 移除 `refreshingElement` 状态下显示的"刷新属性中..."标签

**效果**:
- 每次点击元素 0 次后端请求（之前：1次请求）
- 直接使用 `loadHierarchy` 返回的完整数据
- 点击响应即时，不再有网络延迟

**构建**: ✓ built in 11.76s, CaptureElementDialog chunk: 26.79 kB (之前 27.75 kB)

---

### 第十三次修复 - 截图与层级数据不同步导致元素位置不对 (2026-06-22 16:06)

**问题**: 用户反馈后端数据与屏幕上实际元素对不上，与 weditor 对比完全不对。

**根因**:

1. **两次独立 API 调用**:
   ```
   captureScreen() → POST /screenshot/             → 截图 @ T1
   loadHierarchy() → POST /dump-hierarchy/?skip=1   → 层级 @ T2
   ```
   截图和层级来自不同时刻，屏幕状态可能已变化。

2. **后端执行顺序错误**: 先截图再 dump 层级。如果 uiautomator2 需要安装 ATX agent，中间有几秒延迟，屏幕已变化。

**修复**:

**后端 (device_views.py)**:
- 交换执行顺序：**先 dump 层级，再截图**，确保截图反映的是 dump 后的屏幕状态
- 分辨率获取也移到层级 dump 之后
- 截图失败不中断流程（层级数据仍然可用）
- 新增日志记录截图大小

**前端 (CaptureElementDialog.vue)**:

1. `captureScreen()` 重构:
   - uiautomator 类型：调用 `dumpDeviceHierarchy(skipScreenshot: false)` **单次 API** 获取截图+层级
   - 非 uiautomator 类型：保持 `captureDeviceScreenshot` 单独截图
   - 从返回的 `resolution` 设置 `imageSize` 用于坐标缩放回退

2. `loadHierarchy()`: **始终获取新鲜截图**（不再 skipScreenshot），确保截图和层级同步

3. `getHierarchyRectStyle()`: 缩放时优先使用图片 `naturalWidth`，回退使用后端 `imageSize.value`（屏幕分辨率）

**效果**:
- 截图和层级数据始终来自同一次 API 调用，时间完全同步
- 不再出现截图和层级不匹配的问题
- 与 weditor 行为一致（同时获取截图和层级）

**构建**: ✓ backend 编译通过，frontend built in 16.68s, CaptureElementDialog chunk: 27.26 kB

---

### 第十四次修复 - UI层级定位全面重构为WEditor风格 (2026-06-22 16:14)

**问题**: 用户反馈元素信息完全对不上，要求按照 weditor 的方式全面重构。

**WEditor核心行为**:
1. 截图上点击位置 → 转换为设备坐标 → 遍历所有元素 → 找到包含该点的**最深**(面积最小)叶子节点
2. 选择性高亮单个元素（而不是渲染所有元素矩形）
3. 鼠标悬浮时预览高亮

**后端 (device_views.py)**:

1. `_parse_hierarchy_nodes` **移除过滤条件**：
   - 之前: 只返回 `clickable or checkable or scrollable or resource_id or text or content_desc` 的元素
   - 现在: 返回**所有**有有效 bounds 的节点（与 weditor 一致）
   - 原因：weditor 的点击查找需要遍历完整树来找到最深的包含节点

2. **新增截图分辨率校验**：
   - 解析 PNG IHDR chunk 获取截图实际尺寸
   - 与设备分辨率对比，不一致时以截图尺寸为准
   - 日志记录两者差异，便于诊断坐标偏移问题

**前端 (CaptureElementDialog.vue)**:

1. **模板改造**：
   - 移除 `hierarchy-overlay`（所有元素叠层矩形）- 性能提升，不再渲染数百个 div
   - 新增 `hierarchy-highlight`（单个选中元素高亮，蓝色实线 + 阴影）
   - 新增 `hierarchy-hover-preview`（悬浮预览高亮，橙色虚线）

2. **新增核心函数**：
   - `displayToDeviceCoords(displayX, displayY)` - 显示坐标→设备物理坐标转换
   - `findElementAtPoint(deviceX, deviceY)` - **核心算法**：遍历所有元素，找到包含该点且面积最小的节点（最深叶子节点）
   - `clickToFindElement(displayX, displayY)` - 组合调用，一键点击查找

3. **鼠标事件重构**：
   - `handleMouseDown`: uiautomator模式记录起始点(`action='uiautomator_click'`)，不创建选区
   - `handleMouseMove`: uiautomator非拖拽状态 → hover preview（实时显示鼠标下方元素）
   - `handleMouseUp`: uiautomator模式 → 判断位移<5px为点击 → 调用`clickToFindElement`查找并选中元素

4. **CSS更新**：
   - 移除 `.hierarchy-overlay`, `.hierarchy-element-rect`, `.hierarchy-text-label` 等旧样式
   - 新增 `.hierarchy-highlight`（蓝色实线边框+半透明背景+阴影）
   - 新增 `.hierarchy-hover-preview`（橙色虚线+淡色背景）

5. **类型切换清理**：切出 uiautomator 模式时清空所有层级状态

**效果**:
- 不再渲染所有元素矩形（之前500+ div → 现在0-2 div）
- 点击截图上任意位置 → 精确查找到最深的UI元素
- 鼠标悬浮 → 实时预览元素高亮
- 完全复刻 weditor 的点击查找体验

**构建**: ✓ backend 编译通过，frontend built in 16.91s, CaptureElementDialog chunk: 28.91 kB (增加1.65 kB，新增3个核心函数)

---

### 第十五次修复 - 设备检索不更新最新状态 (2026-06-22 16:57)

**问题**: 用户反馈设备管理、元素管理从设备导入的设备筛选，在检索时不更新最新设备在线状态。

**根因**: `GET /app-automation/devices/` 的 `list()` 方法直接查询数据库返回设备记录，不检查 ADB 实际设备状态。只有手动点击"刷新设备"（调用 `discover`）才会查询 ADB 并更新数据库状态。

**影响范围**:
- `DeviceList.vue` 页面加载和 30 秒定时刷新 → 显示过期状态
- `CaptureElementDialog.vue` 打开时的设备选择下拉 → 显示过期状态
- 任何通过搜索/过滤检索设备的场景 → 显示过期状态

**后端 (device_views.py)**:

1. **新增 `_sync_device_status()` 类方法**：
   - 通过 `DeviceManager.list_devices()` 查询 ADB 当前在线设备
   - 双检锁 + 10 秒缓存（`_SYNC_INTERVAL = 10s`）避免频繁调用 ADB
   - 双向同步：DB 中不在 ADB 的设备 → `offline`；DB 中 `offline` 但在 ADB 的 → `online`
   - 排除 `locked` 状态设备（锁定设备不改变状态）
   - ADB 调用失败时静默跳过，继续返回数据库状态

2. **重写 `list()` 方法**：
   - 返回设备列表前自动调用 `_sync_device_status()`
   - 同步失败不影响结果（降级为返回数据库现状）

3. **增强 `discover()` 方法**：
   - 发现设备后，标记 ADB 中不存在的设备为 `offline`（排除已锁定设备）

**前端**: 无需修改（所有调用方使用 `getDeviceList()` → `list()` 自动同步）

**效果**: 任何时候获取设备列表（页面打开/定时刷新/对话框打开/搜索过滤），都会自动同步 ADB 实际状态。设备 10 秒内多次查询复用缓存，避免频繁 ADB 调用。

---

### 第十六次修复 - 使用设备时离线设备不出现在检索名单 + 前端筛选 (2026-06-22 17:09)

**问题**: 用户反馈使用设备时（如元素管理从设备创建），离线设备不应出现在选择列表中。同时设备管理页面需要前端筛选功能。

**后端 (device_views.py)**:
1. 新增 `AppDeviceFilter(django_filters.FilterSet)` 自定义过滤器：
   - `status` 字段使用 `filter_status` 方法，支持逗号分隔多值筛选（如 `status=online,available,locked`）
   - `connection_type` 保持精确匹配
2. `filterset_fields` → `filterset_class = AppDeviceFilter`

**前端 — DeviceList.vue (设备管理页)**:
1. 新增状态筛选栏：`el-checkbox-group` 多选，默认选中 online/available/locked（排除离线）
2. 新增连接类型筛选：`el-select` 下拉
3. 新增 `buildFilterParams()`: 将筛选条件转为 API 参数（status 逗号拼接）
4. 新增 `onFilterChange()`: 筛选变化时重新请求
5. `getDevices()`: 传入筛选参数
6. `refreshDevices()`: 刷新后按筛选条件重新获取

**前端 — 设备使用场景排除离线**:
- `CaptureElementDialog.vue`: `loadDevices()` 传 `{ status: 'online,available,locked' }`
- `TestCaseList.vue`: `loadDevices()` 传 `{ status: 'online,available,locked' }`
- `SuiteList.vue`: `loadDevices()` 传 `{ status: 'online,available,locked' }`
- `ScheduledTasks.vue`: `loadOptions()` 传 `{ status: 'online,available,locked' }`

**构建**: ✓ backend 编译通过，frontend built in 19.13s

---

### 第十七次功能 - 远程主机设备代理 (Agent Architecture) (2026-06-22 17:51)

**需求**: 其他主机网络访问 TestHub 时，无法使用本地 USB/模拟器设备。需要支持远程主机的本地 ADB 设备连接。

**架构**: WebSocket-based Agent 代理模式

**后端 — 新模型 (models.py)**:
- `AgentHost`: host_id(唯一), hostname, ip_address, port, status(online/offline), version, device_count, extra_info, last_seen
- `AppDevice`: 新增字段 `agent_host`(FK→AgentHost), `agent_device_id`(Agent原始device_id), connection_type 新增 `agent_device`

**后端 — Agent WebSocket Consumer (consumers.py)**:
- 新增 `AgentConsumer(AsyncJsonWebsocketConsumer)`: 处理 Agent 注册/心跳/设备同步/命令执行
- 协议: register → registered → sync_devices → device_sync → ping/pong → exec_command → command_result
- 断开时自动标记该 Agent 的所有设备为离线

**后端 — Agent 注册表 (managers/agent_registry.py)**:
- 新增 `AgentRegistry` 单例: 管理 Agent 连接、消息路由、请求-响应匹配
- `send_to_agent()`: 发送消息到指定 Agent
- `send_and_wait()`: 发送请求并等待异步响应 (asyncio.Future)
- `broadcast_to_agents()`: 向所有 Agent 广播

**后端 — DeviceManager 扩展**:
- `enable_tcp_debug(device_id, tcp_port)`: 为 USB 设备启用 ADB TCP 调试 (adb tcpip)
- `get_adb_tcp_ip(device_id)`: 获取设备 WiFi IP

**后端 — device_views.py 更新**:
- `AppDeviceFilter`: 新增 `agent_host_id` 和 `has_agent` 筛选
- `list()`: 新增 `_sync_agent_devices()` 检查 Agent 超时
- `agent_hosts` action: 获取 Agent 主机列表
- `agent_sync` action: 请求所有 Agent 同步设备
- `screenshot`/`dump_hierarchy`: Agent 设备通过 WebSocket 中继命令
- 新增 `_agent_screenshot()` 和 `_agent_dump_hierarchy()` 方法

**后端 — 路由 (routing.py)**:
- 新增 `ws/app-automation/agent/(?P<host_id>[\w-]*)/$` WebSocket 路由

**后端 — 序列化器 (serializers.py)**:
- 新增 `AgentHostSerializer`
- `AppDeviceSerializer`: 新增 `agent_host_info` 字段

**Agent 脚本 (adb_agent.py)**:
- 独立的 Python 脚本，可部署在远程 Windows/Linux/macOS 主机
- 通过 WebSocket 连接 TestHub 服务器
- 发现本地 ADB 设备 (USB + 模拟器)
- 为 USB 真机自动启用 TCP 调试 (adb tcpip 5555)
- 获取设备 WiFi IP 作为 ADB 连接地址
- 定时同步设备列表 (默认 30s)
- 心跳保活 (默认 15s)
- 处理服务器命令: 截图 (adb screencap)、UI层级 (adb uiautomator dump)
- 支持命令行参数: --server, --host-id, --adb, --tcp-port, --interval, --heartbeat

**前端 — DeviceList.vue**:
- 新增 "Agent主机" 列: 显示主机名/IP/在线状态
- `getConnectionTypeName`: 新增 `agent_device` → "Agent代理"
- `getConnectionTypeTag`: 新增，Agent 设备显示绿色标签
- `isRemoteDevice`: 包含 `agent_device`
- 详情对话框新增 Agent 主机信息行

**数据库迁移**: `0004_add_agent_host` — AgentHost模型 + AppDevice字段

**构建**: ✓ migrate OK, backend 编译通过, frontend built in 21.46s

**使用方式**:
```bash
# 远程主机运行 Agent
pip install websockets
python adb_agent.py --server ws://192.168.1.100:8000
```

---

### 第十八次功能 - Agent 一键下载与安装引导 UI (2026-06-22 18:02)

**需求**: 用户不想在远程主机上手动下载/安装，希望通过 Web UI 一键下载 Agent 并复制启动命令。

**后端 — 下载端点 (urls.py)**:
- `GET /api/app-automation/agent/download/` → FileResponse 下载 `adb_agent.py`
- `GET /api/app-automation/agent/requirements/` → FileResponse 下载 `requirements-agent.txt`
- 新增 `requirements-agent.txt`: 仅依赖 `websockets>=10.0`

**前端 API (app-automation.js)**:
- `getAgentHosts(params)`: GET /devices/agent-hosts/
- `agentSync()`: POST /devices/agent-sync/
- `getAgentDownloadUrl()`, `getAgentRequirementsUrl()`: 返回下载地址

**前端 — DeviceList.vue Agent 安装引导卡片**:
- 设备管理页面顶部新增可折叠卡片 "远程设备代理 (Agent)"
- Agent 在线状态：显示已连接主机数和设备数 + 主机标签列表
- 安装引导 (el-collapse): 3步引导
  - 步骤1: 一键下载 adb_agent.py + 依赖文件 (el-button)
  - 步骤2: 复制安装命令 `pip install -r requirements-agent.txt`
  - 步骤3: 复制启动命令 (自动生成 WebSocket 地址: `python adb_agent.py --server ws://当前服务器地址/ws/app-automation/agent/`)
- 每个命令旁有复制按钮 + tooltip
- 关闭后显示小提示 "远程设备代理设置" 可重新打开
- 连接类型筛选新增 `agent_device` 选项

**构建**: ✓ backend 编译通过, frontend built in 11.93s

**用户操作流程**:
1. 打开设备管理页面 → 看到 Agent 安装引导卡片
2. 点击下载按钮 → 下载 adb_agent.py 和 requirements-agent.txt
3. 点击复制按钮 → 复制安装命令和启动命令
4. 在远程主机粘贴执行 → 设备自动出现在列表中

### 第十九次修复 - Agent WebSocket URL 路径重复拼接导致 404 (2026-06-22 18:10)

**Bug**: Agent 连接服务器时返回 404:
- 日志: `ws://localhost/ws/app-automation/agent/ws/app-automation/agent/PC-xxx/`
- 路径 `/ws/app-automation/agent/` 出现了两次

**根因**:
1. 前端 `agentRunCmd` 生成: `--server ws://localhost/ws/app-automation/agent/` (包含路径)
2. `adb_agent.py` L350: `ws_url = f"{self.server_url}/ws/app-automation/agent/{self.host_id}/"` (再拼接一次)
3. 结果路径重复: `/ws/app-automation/agent/ws/app-automation/agent/host_id/` → 404

**修复**: 前端 `agentRunCmd` 改为只传基础地址:
- 旧: `python adb_agent.py --server ${wsUrl}/ws/app-automation/agent/`
- 新: `python adb_agent.py --server ${wsUrl}`
- Agent 内部自动拼接 `/ws/app-automation/agent/{host_id}/`

**构建**: ✓ frontend built in 11.74s

### 第二十次修复 - WebSocket 仍 404 的根本原因：runserver 不支持 ASGI (2026-06-22 18:14)

**Bug**: Agent URL 路径正确后仍 404:
- 连接: `ws://localhost/ws/app-automation/agent/PC-xxx/` → HTTP 404

**根因**: `start_backend.bat` 使用 `manage.py runserver`（Django WSGI 开发服务器），它**不支持 WebSocket**。WebSocket 升级请求被当作普通 HTTP GET 请求，`/ws/` 路径在 HTTP 路由中不存在 → 404。

**修复**: `start_backend.bat` 改为使用 Daphne (ASGI 服务器):
- 旧: `venv\Scripts\python manage.py runserver 0.0.0.0:8000`
- 新: `venv\Scripts\daphne -b 0.0.0.0 -p 8000 backend.asgi:application`
- `daphne==4.2.1` 已在 `requirements.txt` 中安装
- `stop_all.bat` 无需修改（`taskkill /F /IM python.exe` 同样生效）

### 第二十一修复 - AgentConsumer 注册时崩溃 (1011 internal error) (2026-06-22 18:17)

**Bug**: WebSocket 连接成功、注册发出后立即 1011 断连。

**根因**: `consumers.py` 中 4 处 `from ..models import` 导入路径错误。
- 文件位置: `apps/app_automation/consumers.py`
- `..models` → 解析为 `apps.models`（不存在!）
- `.models` → 解析为 `apps.app_automation.models`（正确, AgentHost/AppDevice 在此定义）

**修复**: 将所有 `from ..models` 改为 `from .models`（4处：disconnect, _handle_register, _handle_device_sync, _get_agent_host）

### 第二十二修复 - 异步上下文中同步 ORM 调用崩溃 (2026-06-22 18:20)

**Bug**: "You cannot call this from an async context - use a thread or sync_to_async."

**根因**: Django Channels async consumer 中直接调用了 Django 同步 ORM（`update_or_create`、`save()`、`filter().update()` 等），在 ASGI 模式下会抛出 `SynchronousOnlyOperation`。

**修复**: 重构 `AgentConsumer`，将所有数据库操作封装为 `@staticmethod` 同步方法，通过 `database_sync_to_async` 包装后调用：

| 方法 | 职责 |
|------|------|
| `_db_get_agent_host(host_id)` | 查询 AgentHost |
| `_db_register_host(host_id, agent_info)` | 创建/更新 AgentHost |
| `_db_mark_agent_offline(host_id)` | 断开时标记离线 |
| `_db_sync_device(...)` | 创建/更新单个设备 |
| `_db_mark_stale_devices(host_id, ids)` | 标记过期设备离线 |
| `_db_update_host_device_count(host_id)` | 更新设备计数 + last_seen |

调用方式: `await database_sync_to_async(self._db_xxx)(args)`

---

### 第二十三修复 - BZPOPMIN 错误导致 WebSocket 1011 断连 (2026-06-22 18:38)

**Bug**: Agent WebSocket 连接后，服务器端报 `redis.exceptions.ResponseError: unknown command 'BZPOPMIN'`，连接被断开 (1011 internal error)。

**根因**: `channels-redis 4.3.0` 内部使用 `BZPOPMIN` Redis 命令管理通道消息队列，但 Windows 上的 Redis 版本太旧（通常是 3.x），`BZPOPMIN` 从 Redis 5.0 才开始支持。

**修复**: 将 `CHANNEL_LAYERS` 从 `RedisChannelLayer` 切换为 `InMemoryChannelLayer`（内存通道层），不再依赖 Redis。

```python
CHANNEL_LAYERS = {
    'default': {
        'BACKEND': 'channels.layers.InMemoryChannelLayer',
    },
}
```

**注意**: `InMemoryChannelLayer` 仅在单进程部署中有效。多进程部署需要升级 Redis 到 5.0+ 并切回 `RedisChannelLayer`。

---

### 第二十四修复 - Agent TCP调试IP错误回退到主机IP (2026-06-22 18:41)

**Bug**: Agent 首次同步设备 IP 正确（`172.26.48.96`），后续同步 IP 变成主机 IP（`172.26.21.107`）。

**根因**: `_sync_devices()` 每次同步都执行 `adb tcpip`（设备已处于TCP模式，输出为空但仍返回True），导致USB连接短暂断开；`get_device_ip` 失败后回退到 `self._local_ip`（主机IP）。

**修复** (`adb_agent.py`):
- 新增 `_tcp_enabled_device_ids` 集合：已执行过 tcpip 的设备不再重复执行
- IP 获取移到 tcpip **之前**（USB 连接稳定时获取）
- 移除 `self._local_ip` 回退：获取不到设备 IP 时保持 USB 模式，不错误使用主机 IP

---

### 第二十五修复 - Agent 设备刷新后离线 + 截图 UnicodeDecodeError (2026-06-22 19:18)

**Bug 1**: Agent 设备连接后显示在线，刷新设备列表后立即变为离线
- **根因1**: `_sync_device_status()` 通过本地 ADB 同步设备在线状态，Agent 设备的 `device_id` 格式为 `agent:host:local_id`，与本地 ADB 返回的 `device_id` 不匹配，被标记为 offline → **已在上轮修复**：添加 `agent_host__isnull=True` 排除 Agent 设备
- **根因2**: `discover` 端点同样未排除 Agent 设备 → **本轮修复**：`discover` 的 offline 计数也添加 `agent_host__isnull=True` 过滤

**Bug 2**: Agent 截图报错 `UnicodeDecodeError: 'gbk' codec can't decode byte 0xd0`
- **根因**: `adb_agent.py` 的 `_run()` 方法统一使用 `text=True`，截图 `exec-out screencap -p` 返回 PNG 二进制数据，Windows 上默认 GBK 解码二进制数据直接崩溃
- **修复** (`adb_agent.py`):
  - `_run()` 新增 `binary=False` 参数：`binary=True` 时用 `text=False` 返回 bytes；`text=True` 时加 `encoding='utf-8', errors='replace'` 防止 GBK 崩溃
  - `screenshot()` 改用 `_run(..., binary=True)` 直接返回 bytes，移除无效的 `latin-1` 编码兜底
- **修复** (`D:\360Downloads\adb_agent.py`)：已同步复制修复后的文件

**需要重启**: 后端 `start_backend.bat` 才能生效

---

### 第二十六修复 - Agent 截图跨事件循环 Future 解析失败 (2026-06-22 19:27)

**Bug**: 请求 `/api/app-automation/devices/14/screenshot/` 截取远程主机 Agent 设备无法正常跑通（超时/无响应）

**根因**: `agent_registry.send_and_wait()` 使用 `asyncio.Future` 实现请求-响应，但存在跨事件循环问题：
- Django 同步视图通过 `get_async_loop()` 创建**临时事件循环 A**
- `send_and_wait` 在循环 A 中创建 `asyncio.Future`
- Agent 响应时，Daphne/Channels 的 WebSocket Consumer 在 **Daphne ASGI 事件循环 B** 中调用 `resolve_response()`
- 从循环 B 调用 `future.set_result()` 设置循环 A 的 Future → 跨循环失败，截图永不返回（超时）

**修复方案**: 用 `threading.Event` 替代 `asyncio.Future` 实现跨事件循环安全通信

**修改文件 1** - `agent_registry.py`:
- `AgentConnection.__init__`: 新增 `_sync_events` (dict) 和 `_sync_results` (dict)
- `unregister()`: 注销时通知所有同步等待者
- `send_and_wait()`: 从 async 改为 sync 方法，使用 `threading.Event` 等待响应；发送消息通过 `asyncio.ensure_future` 或临时循环完成
- `resolve_response()`: 优先检查 `_sync_events`，设置 `_sync_results` 后 `event.set()`；兼容旧 asyncio.Future

**修改文件 2** - `device_views.py`:
- `_agent_exec_command()`: 从 `async def` 改为 `def`，直接调用 `agent_registry.send_and_wait(sync)`
- `_agent_screenshot()`: 移除 `loop.run_until_complete`，直接调用 sync `_agent_exec_command`
- `_agent_dump_hierarchy()`: 同上移除 `loop.run_until_complete`

**需要重启**: 后端 `start_backend.bat`

---

### 第二十七修复 - 跨线程 WebSocket 发送导致隧道域名截图失败 (2026-06-22 19:45)

**Bug**: 通过 cpolar 隧道域名访问时截图/层级获取总是失败超时，但内网 IP 直接访问正常

**根因**: `send_and_wait()` 虽然等待部分已用 `threading.Event` 修复，但**发送部分**仍有跨线程问题：
- Django 同步视图运行在**线程池线程**
- WebSocket transport 属于 **Daphne 事件循环线程**
- `send_and_wait` 创建临时事件循环，在其中调用 `conn.consumer.send_json(message)`
- `consumer.send_json()` 最终调用 WebSocket transport，而 transport 绑定在 Daphne 事件循环上
- 从临时循环调用不同循环的 transport → 消息可能从未实际发出去
- 内网 IP 低延迟 → 竞态条件碰巧通过；隧道额外延迟 → 总是失败

**修复方案**: 使用 `asyncio.run_coroutine_threadsafe()` 将 WebSocket 发送调度到 Daphne 事件循环

**修改文件 1** - `agent_registry.py`:
- `__init__()`: 新增 `_event_loop` 属性，存储 Daphne 事件循环引用
- `capture_event_loop()`: 新增方法，由 consumer 在 connect 时调用
- `send_and_wait()`: 优先使用 `run_coroutine_threadsafe()` 将 `send_to_agent` 调度到 Daphne 循环；仅当 Daphne 循环不可用时才回退到临时循环
- `broadcast_sync()`: 新增方法，同样使用 `run_coroutine_threadsafe` 进行跨线程广播

**修改文件 2** - `consumers.py`:
- `AgentConsumer.connect()`: 启动时调用 `agent_registry.capture_event_loop()` 捕获 Daphne 循环

**修改文件 3** - `device_views.py`:
- `agent_sync()`: 改用 `agent_registry.broadcast_sync()` 替代 `loop.run_until_complete(broadcast_to_agents())`

**需要重启**: 后端 `start_backend.bat`

---

### 第二十八次优化 - Agent 截图/层级获取性能优化 (2026-06-22 19:52)

**问题**: 隧道域名访问时截图 15s 超时，UI 层级 22s 响应太慢

**根因分析**:
- 截图: ADB screencap (3-5s) → base64 编码 → WebSocket 传输大体积 JSON（PNG ~2-5MB）
- 层级: `uiautomator dump`(5-8s) + `pull XML`(2-3s) + `get_resolution`(2s) + `screencap`(3-5s) = **全部串行执行**，总耗时 15-22s
- 隧道额外延迟放大了传输时间

**优化方案**:

| 优化项 | 预期收益 | 说明 |
|--------|---------|------|
| 并行执行 | -40~50% 层级时间 | dump_hierarchy + screenshot + resolution 并发 |
| gzip 压缩 XML | -80~95% XML 传输体积 | XML 文本压缩率极高 |
| PNG→JPEG 转换 | -80~90% 截图传输体积 | 可选依赖 Pillow，无 Pillow 时回退 gzip PNG |
| skip_screenshot 支持 | -30~50% 层级时间 | 前端已有截图时跳过 Agent 截图 |
| 超时调整 | 减少误报 | 截图 15s→25s，层级 30s→45s |

**修改文件 1** - `adb_agent.py`:
- 新增 import: `gzip`, `base64`(模块级), 可选 `PIL.Image`
- 新增 `compress_data(bytes) → base64 str`: gzip 压缩二进制数据
- 新增 `compress_text(str) → base64 str`: gzip 压缩文本
- `_cmd_screenshot()`: Pillow 可用时 PNG→JPEG(quality=70)，否则 gzip 压缩 PNG；返回 `format`/`compressed` 标记
- `_cmd_dump_hierarchy()`: 
  - 使用 `asyncio.gather` 并行执行 dump + screenshot + resolution（`run_in_executor` 线程池）
  - 支持 `skip_screenshot` 参数跳过截图
  - XML 经 gzip 压缩后 base64 传输
  - 截图同样 JPEG/gzip 压缩
  - 返回 `xml_compressed`/`screenshot_format`/`screenshot_compressed` 标记
- `_handle_command()`: 传递 msg 到 `_cmd_dump_hierarchy`

**修改文件 2** - `device_views.py`:
- 新增 import: `gzip`
- 新增 `_decompress_if_needed(data)`:
  - 自动解压 `compressed='gzip'` 的截图 content
  - 自动解压 `xml_compressed='gzip'` 的 XML
  - 自动解压 `screenshot_compressed='gzip'` 的层级截图
  - 向后兼容旧版未压缩数据
- `_agent_screenshot()`: timeout 15s→25s，调用 `_decompress_if_needed()`
- `_agent_dump_hierarchy()`: timeout 30s→45s，调用 `_decompress_if_needed()`，支持 `skip_screenshot` 参数，处理 JPEG/PNG 格式前缀
- `dump_hierarchy()`: Agent 设备也读取 `skip_screenshot` 参数并传递给 agent

**向后兼容**: 旧版 Agent（无压缩）返回的数据没有 `compressed` 字段，`_decompress_if_needed` 直接透传不处理。

**建议**: 远程主机 Agent 安装 Pillow 获得最佳性能: `pip install Pillow`

**需要重启**: 后端 `start_backend.bat`，Agent 重新部署 `adb_agent.py`

---

### 第二十九次修复 - Python 3.13 asyncio.create_task() 拒绝 Future 导致层级获取 500 (2026-06-22 20:05)

**Bug**: `POST /api/app-automation/devices/16/dump-hierarchy/` 返回 500:
```
"a coroutine was expected, got <Future pending cb=[_chain_future.<locals>._call_check_cancel() at ...]>"
```

**根因**: `adb_agent.py` 的 `_cmd_dump_hierarchy()` 使用 `asyncio.create_task(loop.run_in_executor(...))` 包装。在 Python 3.13 中，`asyncio.create_task()` 不再接受 Future 对象（`run_in_executor` 返回的是 asyncio.Future，而非 coroutine），直接抛出 TypeError。

**修复** (`adb_agent.py`):
- 移除 `asyncio.create_task()` 包装
- `loop.run_in_executor()` 返回的 Future 本身就是 awaitable，直接存入 dict 后用 `await` 等待即可
- 3 个 Future（dump/resolution/screenshot）仍然并行执行（`run_in_executor` 调度到线程池后立即返回）

```python
# 旧（Python 3.13 报错）
tasks['dump'] = asyncio.create_task(loop.run_in_executor(None, self.adb.dump_hierarchy, device_id))

# 新（Python 3.13 兼容）
futures['dump'] = loop.run_in_executor(None, self.adb.dump_hierarchy, device_id)
```

**需要重启**: Agent 重新部署 `adb_agent.py`

---

### 第三十次修复 - discover 端点不触发 Agent 同步、不计算 Agent 主机数量 (2026-06-22 20:10)

**Bug**: 设备管理页面点击"刷新设备"时提示 "发现 0 个设备"，而 Agent 主机实际上有连接且有设备。

**根因**: `discover` 方法只调用 `manager.list_devices()`（本地 `adb devices -l`），完全忽略 Agent 代理设备：
- 不触发 Agent 主机设备同步
- 响应消息中不包含 Agent 设备数量
- 不统计 Agent 主机在线数量

**修复** (`device_views.py` - `discover()`):

1. **触发 Agent 同步**: `agent_registry.broadcast_sync({"type": "sync_devices"})` 向所有活跃 Agent 发送设备同步请求
2. **统计 Agent 设备数**: `AppDevice.objects.filter(agent_host__isnull=False).exclude(status='offline').count()`
3. **统计在线 Agent 主机数**: `AgentHost.objects.filter(status='online').count()`
4. **响应增强**:
   - 消息: `"发现 X 个本地设备，Y 个Agent设备 (Z 个Agent主机在线)，已向 N 个 Agent 主机发送同步请求"`
   - 新增字段: `total_devices`, `local_device_count`, `agent_device_count`, `agent_host_count`

**需要重启**: 后端 `start_backend.bat`

---

### 第三十一次修复 - Agent 区域移到刷新设备按钮旁，改为弹窗显示 (2026-06-22 20:15)

**需求**: 将"远程设备代理 (Agent)"从页面卡片移到头部按钮旁，点击弹出对话框显示。

**修改** (`DeviceList.vue`):

1. **头部新增按钮**: 在"刷新设备"和"添加远程设备"旁添加"远程设备代理"按钮，带 `el-badge` 徽标显示在线 Agent 主机数量
2. **移除页面内嵌卡片**: 删除原来的 `el-card` 卡片区域和关闭后的"远程设备代理设置"文字按钮
3. **新增弹窗**: `el-dialog` 替代卡片，包含 Agent 在线状态（`el-alert` + `el-tag` 列表）和安装指南（下载/安装/启动三步）

**State 变更**: `showAgentGuide` + `agentCollapseActive` → `showAgentDialog`
**移除导入**: `Close` 图标（不再使用）

---

### 第三十二次修复 - Agent 主机只显示在线，离线不保存不显示 (2026-06-22 20:21)

**需求**: Agent 主机列表中离线主机不应保存和显示，只保留在线主机。

**后端修改**:

1. `device_views.py` - `agent_hosts` 端点: `AgentHost.objects.all()` → `AgentHost.objects.filter(status='online')`，只返回在线主机
2. `device_views.py` - `_sync_agent_devices()`: 超时主机不再标记为离线，改为直接删除，同时解除设备关联 (`agent_host=None`)
3. `consumers.py` - `_db_mark_agent_offline()`: Agent 断开时直接删除主机记录而非标记离线，解除设备关联
4. `consumers.py` - `_db_mark_stale_devices()`: 标记过期设备时同时解除 `agent_host=None`

**前端修改** (`DeviceList.vue`):
- 移除 `agentOnlineCount` 计算属性，直接用 `agentHosts.length`
- 主机标签 `el-tag` type 固定为 `"success"`（全部在线）
- 按钮徽标 `el-badge` 直接用 `agentHosts.length`

**需要重启**: 后端 `start_backend.bat` + 前端 `npm run build`

---

### 第三十三次修复 - Agent 主机名称显示不全 (2026-06-22 20:26)

**Bug**: Agent 弹窗中主机显示为 "DJ (1台)" 而非完整的 "DJ-bf7a2991 (1台)"

**根因**: `adb_agent.py` 发送的 `hostname` 是 `socket.gethostname()` 只返回短主机名（如 "DJ"），而 `host_id` 才是完整的 `"主机名-8位UUID"`（如 "DJ-bf7a2991"）。前端三处显示都使用了 `hostname` 而非 `host_id`。

**修复** (`DeviceList.vue`):
1. Agent 弹窗主机标签: `host.hostname || host.ip_address` → `host.host_id`
2. 设备表 "Agent主机" 列: `hostname` → `host_id`（同时移除冗余 tooltip）
3. 设备详情弹窗: `hostname` → `host_id`

**需要重启**: 前端 `npm run build`

---

### 第三十四次修复 - connection_type 筛选 400 错误 (2026-06-22 20:28)

**Bug**: `GET /api/app-automation/devices/?connection_type=usb` 和 `connection_type=remote` 返回 400: "usb/remote 不在可用的选项中"

**根因**: 模型 `CONNECTION_TYPE_CHOICES` 只有 `emulator/remote_emulator/real_device/agent_device` 四种，但 `discover` 接口在写入设备时却使用了 `usb` 和 `remote`（第344-345行），导致前端按这些值筛选时 Django Filter 的 ChoiceField 校验失败。

**修复** (`models.py`):
- `CONNECTION_TYPE_CHOICES` 新增 `('usb', 'USB连接')` 和 `('remote', '远程连接')`
- 生成迁移: `0005_add_usb_remote_connection_types.py`
- 迁移已应用: `python manage.py migrate app_automation`

**需要重启**: 后端 `start_backend.bat`<｜end▁of▁thinking｜>现在写入工作记忆：

<｜｜DSML｜｜tool_calls>
<｜｜DSML｜｜invoke name="read_file">
<｜｜DSML｜｜parameter name="filePath" string="true">e:\wk\wk_python\testhub_platform-main\.codebuddy\memory\MEMORY.md
